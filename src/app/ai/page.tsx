"use client";

// import styled, { css } from "styled-components";
// import { Send, AddPhotoAlternate } from "@mui/icons-material";
// import { AppColors } from "@/styles/colors";
// import { AppTextStyles } from "../../styles/textStyles";
// import { Suspense } from "react"; // Suspense 제거
// import { useState, useEffect } from "react";
// import { AiChatQuestion } from "@/components/Ai/AiChatQuestion";
// import { AiProgressBar } from "@/components/Ai/AiProgressBar";
// import { customScrollbar } from "@/styles/commonStyles";
// import { useRouter, usePathname, useSearchParams } from "next/navigation";
// import AiChatPage from "./[conversationId]/page";

// --- 데이터 정의 (주석 처리) ---
/*
const stepData = [
  // ... (stepData 내용)
];
*/

// --- 스타일 컴포넌트 (주석 처리) ---
/*
const Container = styled.div` ... `;
const MainContent = styled.div` ... `;
const ChatContainer = styled.div` ... `;
const FlexContainer = styled.div` ... `;
const Title = styled.p` ... `;
const Subtitle = styled.p` ... `;
const ChatContent = styled.div` ... `;
const CenterContent = styled.div` ... `;
const ProfileImage = styled.img` ... `;
const MessageInput = styled.div` ... `;
const InputContainer = styled.div` ... `;
const IconContainer = styled.button` ... `;
const Input = styled.input` ... `;
const FreeFormGuide = styled.div` ... `;
*/

export default function AiPage() {
  // AiChatPage 관련 코드 주석 처리
  /*
  return (
    <Suspense fallback={<div>로딩 중...</div>}>
      <AiChatPage params={{ conversationId: "mock_conversation_id" }} />
    </Suspense>
  );
  */

  // 빌드 오류를 피하기 위해 null 또는 빈 Fragment 반환
  return null;
  // 또는 return <></>;
}
